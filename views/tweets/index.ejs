<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:ital,wght@0,400;0,700;1,200&display=swap" rel="stylesheet">
    <title>HOME PAGE</title>
</head>
<body>
    <%- include('partials/nav') %>
        <main class="container">
            <h1>List of all Tweets</h1>
            <ul class="tweet-container">
                <% for(let tweet of tweets){ %>
                    <li>
                        <%= tweet.tweet %>  - <b><%= tweet.username %> </b>
                        <a href="/tweets/<%= tweet.id %>" class="btn btn-small">View</a>
                    </li>
                <% } %>
            </ul>
            <a href="/tweets/new" class="btn btn-med">Create a new tweet</a>
        </main>
    <%- include('partials/footer') %>